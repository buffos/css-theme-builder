import type { ThemeConfig } from './types';

export const emitUtilities = (_config: ThemeConfig): string => {
  // TODO: generate utility classes (spacing, typography, colors, radius, shadow).
  return '/* utilities */';
};
