import type { ThemeConfig } from './types';

export const emitComponents = (_config: ThemeConfig): string => {
  // TODO: generate component styles (buttons, inputs, card, alert, table, modal).
  return '/* components */';
};
